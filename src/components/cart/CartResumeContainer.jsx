import React from "react";
import "./styles.css";

const CartResumeContainer = ({children}) => {
  return <div className="cart--resume__container">{children}</div>;
};

export default CartResumeContainer;
