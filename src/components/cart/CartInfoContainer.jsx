import React from "react";
import "./styles.css";

const CartInfoContainer = ({ children }) => {
  return <div className="cart--info__container">{children}</div>;
};

export default CartInfoContainer;
